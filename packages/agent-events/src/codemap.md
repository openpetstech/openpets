# packages/agent-events/src/

## Files

- **index.ts**: Speech pools, selection logic, and validation. Exports `HookSpeechCategory` type, `hookSpeechPools` constant, `pickHookSpeech()`, and `validateHookSpeech()`.
- **check-agent-events.ts**: Contract validation (excluded from detailed documentation).
