# packages/cli/src/

## Files

- **index.ts**: Main CLI entry (403 lines). Command routing, `configureProject()`, `installPetFromCatalog()`, `runMcp()`, `runClaudeHookFromStdin()` wrapper. All argument parsing, pet resolution, hook installation, and safety checks.
- **check-cli-contract.ts**: Contract validation (excluded from detailed documentation).
